# 安全说明

## 报告安全问题

请优先使用 GitHub 的私密安全报告功能联系维护者。不要在公开 Issue 中发布可复现的未修复漏洞、有效令牌、服务器公网地址、玩家 IP 或玩家存档数据。

报告应包含受影响版本、影响范围、最小复现步骤和已做的缓解措施。附件中的日志必须先脱敏。

## 部署要求

- REST API 默认保持关闭，确认令牌和 IP 白名单后再启用。
- 默认只绑定 `127.0.0.1`；跨主机访问应通过防火墙或可信反向代理限制来源。
- 服务本身不提供 TLS，不应直接暴露到公网。
- 每个后台或自动化服务使用独立令牌，并只授予所需权限。
- 令牌泄露后应立即禁用或删除对应令牌文件并重启服务器。
- 限制 `dlls/RESTAPI/Tokens`、`dlls/Logs` 和 `dlls/Pals` 的文件访问权限。

## 支持范围

只支持维护者通过独立发布页面提供的正式模组版本。自行修改、反编译或通过第三方重新打包的版本不在安全支持范围内。本仓库不分发模组文件。
